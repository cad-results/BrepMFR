name: new_brepmfr
channels:
  - pytorch
  - dglteam/label/cu121
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch>=2.5
  - pytorch-cuda=12.1
  - pythonocc-core>=7.9
  - PySide2
  - pip
  - pip:
      - pytorch-lightning>=2.4
      - torch-geometric>=2.5
      - numpy>=1.26,<2.0
      - torchmetrics>=1.0
      # fairseq>=0.12.3  # Only needed for original BrepMFR (models/), may require Python <=3.10
